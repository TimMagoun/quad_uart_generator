# Agent Instructions

- Always use `uv` to create and manage Python environments and dependencies.
- Always use project-local virtual environments (for example, `.venv`) instead of system Python installs.
- Do not install Python packages globally unless explicitly requested.
- There is a RP2040 pro micro device attached via USB to serial. Use it to flash the device with the firmware.
